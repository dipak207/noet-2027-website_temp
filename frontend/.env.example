# Local development: Django dev server running on localhost:8000
# Production: Set VITE_API_URL to your production API URL (e.g., https://api.example.com)
#
# This file shows the expected environment variables.
# For local development, create .env.local or .env.development with local values.
# For production deployment on Vercel, configure VITE_API_URL in environment variables.

VITE_API_URL=http://localhost:8000

